<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
        <title>Lijun Zhu</title>
        <link>{{ config.base_url }}</link>
        <description>Tech leadership insights, AI innovation, and team building expertise from a Director of Computer Vision</description>
        <generator>Zola</generator>
        <language>{{ config.default_language }}</language>
        <atom:link href="{{ get_url(path="rss.xml", trailingslash=false) }}" rel="self" type="application/rss+xml"/>
        <lastBuildDate>{{ last_updated | date(format="%a, %d %b %Y %H:%M:%S %z") }}</lastBuildDate>
        <image>
            <url>{{ get_url(path="rss-icon.png") }}</url>
            <title>Lijun Zhu</title>
            <link>{{ config.base_url }}</link>
            <width>144</width>
            <height>144</height>
            <description>Lijun Zhu - Tech leadership insights and AI innovation</description>
        </image>
        {%- for page in pages %}
        <item>
            <title>{{ page.title }}</title>
            <pubDate>{{ page.date | date(format="%a, %d %b %Y %H:%M:%S %z") }}</pubDate>
            {%- if config.extra.author %}
            <author>gatechzhu@gmail.com ({{ config.extra.author }})</author>
            {%- else %}
            <author>noreply@example.com</author>
            {%- endif %}
            <link>{{ page.permalink }}</link>
            <guid>{{ page.permalink }}</guid>
            <description xml:base="{{ page.permalink }}">{{ page.content | striptags | escape }}</description>
        </item>
        {%- endfor %}
    </channel>
</rss>
